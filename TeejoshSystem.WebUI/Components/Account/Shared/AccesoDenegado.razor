@inject NavigationManager Navigation

<section class="auth-card" role="alert">
    <h1>Acceso denegado</h1>
    <p>Su usuario no tiene los permisos necesarios para acceder a esta seccion.</p>
    <button class="btn primary" @onclick="IrAlInicio">Ir al inicio</button>
</section>

@code {
    private void IrAlInicio() => Navigation.NavigateTo("/");
}
