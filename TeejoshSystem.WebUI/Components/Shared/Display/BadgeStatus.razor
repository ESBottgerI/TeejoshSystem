<span class="badge @CssClass">@Text</span>

@code {
    [Parameter] public int Stock { get; set; }
    private string Text => Stock <= 0 ? "Sin stock" : Stock <= 3 ? "Stock bajo" : "Disponible";
    private string CssClass => Stock <= 0 ? "danger" : Stock <= 3 ? "warning" : "success";
}
