global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - rules.yml

scrape_configs:
  - job_name: "teejosh-blazor"

    metrics_path: /metrics

    static_configs:
      - targets:
          - localhost:5000